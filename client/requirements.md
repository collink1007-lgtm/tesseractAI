## Packages
framer-motion | Page transitions and smooth message animations
react-markdown | Rendering markdown from AI responses
date-fns | Formatting message timestamps
clsx | Conditional class name joining
tailwind-merge | Merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}

The application assumes a dark-mode first professional aesthetic.
